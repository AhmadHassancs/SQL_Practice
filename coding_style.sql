use sales;
create table if not exists test (numbers int(10), words varchar(222));

CREATE TABLE IF NOT EXISTS test (
    numbers INT(10),
    words VARCHAR(222)
);

CREATE TABLE IF NOT EXISTS test (
    numbers 	INT(10),
    words 		VARCHAR(222)
);

/* this is how comments are added
it can be multi line*/

# this is also a comment
-- this is also a comment





